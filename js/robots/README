Unfortunately these files are here only as a reference because
we need to have them in the index.html markup for the game to work :-(
(loading them into index.html from external files is, locally, tricky)